spring:
  application:
    name: main-service
  config:
    import: "optional:configserver:"
  datasource:
    username: user
    password: psw
    url: jdbc:postgresql://localhost:5432/tasksoft_db
  jpa:
    hibernate:
      ddl-auto: validate #it was update before
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#        show_sql: true

#  sql:
#    init:
#      mode: always #tell spring to load the schema.sql file

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080

server:
  port: 9001
  tomcat:
    connection-timeout: 2s
    keep-alive-timeout: 15s
    threads:
      max: 50
      min-spare: 5


